import React from "react";

export default function FrontendQuestions() {
  return (
    <div>
      <h1 className="text-3xl">Frontend Interview Questions</h1>
      <span className=" text-lg text-white/50">
        Master the exact kinds of questions tech companies ask in frontend
        interviews — from logic-building with React to UI bugs, layout
        debugging, and async flows.
      </span>
    </div>
  );
}
